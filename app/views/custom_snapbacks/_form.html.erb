<%= form_with(model: custom_snapback, local: true) do |form| %>
  <% if custom_snapback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(custom_snapback.errors.count, "error") %> prohibited this custom_snapback from being saved:</h2>

      <ul>
      <% custom_snapback.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :custom_snapback_image %>
  </div>

  <div class="field">
    <%= form.label :logo %>
    <%= form.text_field :logo, id: :custom_snapback_logo %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :custom_snapback_title %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :custom_snapback_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
